apiVersion: v1
kind: Pod
metadata:
  name: env-var-pod
spec:
  containers:
    - name: myapp-container
      image: busybox
      command: ["sh", "-c", "echo \"환경 변수 값: $MY_ENV_VAR\" && sleep 3600"]
      env:
        - name: MY_ENV_VAR
          value: "Hello, Kubernetes!"
